import { ColumnstitlePipe } from './columnstitle.pipe';

describe('ColumnstitlePipe', () => {
  it('create an instance', () => {
    const pipe = new ColumnstitlePipe();
    expect(pipe).toBeTruthy();
  });
});
